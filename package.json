{
  "name": "frontend-unsplash",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint ./src/**/*.ts",
    "pretest": "npm run lint",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.7",
    "ts-jest": "^29.1.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
